{
    "version": 2,
    "buildCommand": "pnpm build",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/main.js"
        }
    ],
    "env": {
        "NODE_ENV": "production"
    }
}
